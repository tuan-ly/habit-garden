# Legacy Local Migrations

These 18 SQL files are the pre-ledger local history archived on 2026-07-16. They are evidence only and must not be executed by Supabase CLI.

The active `supabase/migrations/` directory was rebuilt from `supabase migration fetch --linked`, preserving all 51 remote versions exactly. Three verified local changes were then reintroduced with versions after the remote baseline.

Do not move these files back into the active directory or mark their old versions as applied. Consult `docs/SUPABASE-MIGRATION-LEDGER.md` for the reconciliation record.
