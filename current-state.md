# Current State

- Date: 2026-08-30
- Milestone: Retention Core — Daily Habit Notifications
- Active task: Web Push authenticated-device acceptance smoke
- Status: production rollout and local development guard deployed; the remaining release gate is a real browser/PWA subscription and system-notification preview
- Maintenance completed: made the Garden return reassurance dismissible and cleared it after the first successful care action, so experienced users do not retain a stale welcome badge during a session.
- Vertical slice: database-owned daily reminders create durable inbox rows; each browser can register a push subscription; an inbox insert creates one auditable delivery per device; Supabase Cron invokes a secret-authenticated Edge Function every minute; VAPID Web Push reaches the service worker when the PWA is closed; Capacitor Local Notifications remain the native fallback
- Non-goals: email/SMS delivery; third-party push provider; AI-generated copy; escalating nag sequences; changing goal/progression semantics; exposing delivery audit to client roles
- Completed: `push_subscriptions` owner RLS and authenticated actions; private delivery queue; notification enqueue trigger; Vault-backed cron invoker; retrying Edge Function with stale-claim recovery and expired-subscription cleanup; PWA push/click worker; Settings states for unsupported/unconfigured/denied/subscribed devices; per-device enable/disable and preview; foreground duplicate suppression; production VAPID key pair and protected secret storage; Edge Function deployment; migration-ledger apply; Vercel production config and redeploy; service-worker readiness fix that awaits an active worker before Push API subscription; local development guard that disables Web Push when `next-pwa` is disabled; 10-second production readiness timeout; deployment guide, ADR 007 and OpenWiki updates
- Verification: migration ledger has 66 aligned local/remote versions through `20260829221041`; GitHub workflow run `33281295863` succeeded from master commit `e62f22d`; remote catalog confirms both Web Push tables, RLS, enqueue trigger, active cron and revoked client delivery access; latest cron run succeeded and latest `pg_net` response returned HTTP `200`; Vercel deployment for master commit `8ea8944` completed successfully; `https://www.habien.com`, `/sw.js` and the imported push worker return HTTP `200`, with push and notification-click handlers present; service-worker readiness contract test passes; root TypeScript and production build pass
- Constraints: browser permission and subscription are per device; permission must follow a user gesture; iOS/iPadOS Web Push requires a supported installed Home Screen PWA; secrets must never enter Git or client bundles; rotating the VAPID pair requires affected devices to subscribe again
- Risks: a subscription endpoint remains globally unique, so account switching on one browser may require explicit unsubscribe/re-subscribe handling; local Edge HTTP smoke is blocked by the stopped legacy Kong gateway on port `54321`; full repo lint still contains unrelated legacy errors/generated noise; local PL/pgSQL lint retains three unrelated legacy function errors; native packaging and real-device smoke remain separate release gates
- Blockers: the available production browser sessions are not authenticated; final permission/subscription smoke requires the user to sign in and approve the browser or operating-system notification prompt
- Next smallest step: use the production/PWA URL rather than `next dev`, sign in, choose **Bật thông báo**, approve permission and confirm a preview or due reminder outside the open app
