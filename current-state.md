# Current State

- Date: 2026-08-30
- Milestone: Retention Core — Daily Habit Notifications
- Active task: Web Push delivery layer for desktop/PWA notifications — production rollout in progress
- Status: implementation and local verification complete; VAPID Edge secrets, Vault invoker secrets and `push-notifications` Edge Function are deployed, while migration `20260829221041` and Vercel public-key environment remain pending
- Vertical slice: database-owned daily reminders create durable inbox rows; each browser can register a push subscription; an inbox insert creates one auditable delivery per device; Supabase Cron invokes a secret-authenticated Edge Function every minute; VAPID Web Push reaches the service worker when the PWA is closed; Capacitor Local Notifications remain the native fallback
- Non-goals: email/SMS delivery; third-party push provider; AI-generated copy; escalating nag sequences; changing goal/progression semantics; exposing delivery audit to client roles
- Completed: `push_subscriptions` owner RLS and authenticated actions; private delivery queue; notification enqueue trigger; Vault-backed cron invoker; retrying Edge Function with stale-claim recovery and expired-subscription cleanup; PWA push/click worker; Settings states for unsupported/unconfigured/denied/subscribed devices; per-device enable/disable and preview; foreground duplicate suppression; deployment guide; ADR 007 and OpenWiki updates
- Verification: migration ledger has 66 unique versions; focused notification tests pass 14/14; root TypeScript and scoped ESLint pass; Deno check passes; migration applies to local Postgres; catalog checks confirm RLS, trigger, active cron and revoked client access; ERROR-level local advisors report no issues; transactional Web Push SQL probe verifies enqueue, safe idle without Vault secrets and retained audit; Edge Runtime bundles/starts with temporary VAPID keys; production build passes and generated service worker contains push/click handlers
- Constraints: production still needs `NEXT_PUBLIC_WEB_PUSH_VAPID_PUBLIC_KEY` in Vercel plus protected migration-ledger apply; browser permission is per device; iOS/iPadOS Web Push requires a supported installed Home Screen PWA; secrets must never enter Git or client bundles
- Risks: a subscription endpoint remains globally unique, so account switching on one browser may require explicit unsubscribe/re-subscribe handling; local Edge HTTP smoke is blocked by the stopped legacy Kong gateway on port `54321`; full repo lint still contains unrelated legacy errors/generated noise; local PL/pgSQL lint retains three unrelated legacy function errors; native packaging and real-device smoke remain separate release gates
- Blockers: Vercel requires an authenticated dashboard/CLI session before the public key can be added; production delivery cannot be exercised until migration and hosting environment are deployed
- Next smallest step: publish the verified branch, apply migration `20260829221041` through the protected workflow, then add the stored VAPID public key to Vercel and redeploy
