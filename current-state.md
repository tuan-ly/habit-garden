# Current State

- Date: 2026-08-27
- Milestone: Retention Core — Daily Habit Notifications
- Active task: daily habit reminder, warning and goal notification system — Production database deployed; application release pending
- Status: implemented and locally verified; migration `20260823143710` is applied to linked Supabase production through the approved migration-ledger workflow
- Vertical slice: each plant can own a daily reminder time; Supabase Cron creates one timezone-aware, goal-aware inbox row per plant/local date; the dashboard exposes a persistent notification center; mobile shells mirror enabled schedules with Capacitor local notifications and suppress a still-pending reminder after successful completion
- Non-goals: email/SMS delivery; third-party push provider; AI-generated copy; escalating nag sequences; changing goal/progression semantics
- Completed: per-habit enable/time controls in creation and Settings; global reminder switch; goal/capability target copy; unread inbox and safe habit links; idempotent `dedupe_key`; private invoker cron dispatcher; cross-midnight due window; authenticated read/update-only notification grants; Android/iOS local-notification registration; client completion event boundary; browser notification permission flow; production migration deployed from commit `a1f93c3` by workflow run `33083824941`
- Verification: focused notification/reading tests pass; full Vitest reaches 420/421 with only the pre-existing Windows CRLF-sensitive capability migration assertion; isolated asset-studio timeout rerun passes; TypeScript passes; scoped ESLint has zero errors and three pre-existing `add-plant-dialog.tsx` hook warnings; production build passes; migration ledger has 65 unique versions; local SQL probe verifies incomplete goal, completed simple habit, idempotent retry, target `30`, and `23:55` cross-midnight dispatch; remote ledger contains `20260823143710`; linked dry-run is up to date; `habit-reminder-dispatcher` is active every five minutes and its first verified run succeeded at `2026-08-27 14:50:00+00`; the private dispatcher is security-invoker with an empty search path and no execute privilege for `anon` or `authenticated`; `notifications` RLS, `dedupe_key`, its partial unique index, authenticated SELECT and column-scoped UPDATE are present; linked ERROR-level advisors report no issues; Capacitor detects Local Notifications on Android/iOS; in-app Browser verifies `/settings` and notification center at `1280×720` and `390×844` with zero console errors
- Constraints: web system notifications require Habit Garden to remain open, while the database inbox remains durable; native schedules use device-local time and only cancel today's weekday slot when its reminder has not fired; existing notification history remains read-only except for the `read` flag; database deployment intentionally precedes application rollout as an additive compatibility change
- Risks: a native weekday slot cancelled before firing is restored on the next app launch/settings sync; full `cap sync` still needs an `out/index.html` web artifact that the current dynamic Next build does not produce; full repo lint still contains unrelated legacy errors and generated-asset noise; local database lint retains three unrelated legacy function errors; authenticated two-plant session smoke from the prior milestone remains outstanding
- Blockers: application rollout still requires a resolved mobile web-asset packaging path and real-device notification permission/scheduling smoke
- Next smallest step: commit and release the remaining notification application changes against the deployed additive schema
