# Current State

- Date: 2026-08-30
- Milestone: Retention Core — Daily Habit Notifications
- Active task: Daily notification authenticated-device acceptance smoke
- Status: iOS active-worker wait and same-day reminder rescheduling are deployed to production; database and delivery pipeline verification pass
- Maintenance completed: made the first-run Garden onboarding complete on every dismissal path and skip accounts with existing XP, so experienced users do not receive the welcome screen again after local browser state is lost.
- Maintenance verification: focused onboarding tests (4), scoped ESLint and root TypeScript all pass; the regular production build is reserved for the active Web Push worktree because its dev process holds the shared `.next` lock.
- Vertical slice: database-owned daily reminders create durable inbox rows; each browser can register a push subscription; an inbox insert creates one auditable delivery per device; Supabase Cron invokes a secret-authenticated Edge Function every minute; VAPID Web Push reaches the service worker when the PWA is closed; Capacitor Local Notifications remain the native fallback
- Non-goals: email/SMS delivery; third-party push provider; AI-generated copy; escalating nag sequences; changing goal/progression semantics; exposing delivery audit to client roles
- Completed: `push_subscriptions` owner RLS and authenticated actions; private delivery queue; notification enqueue trigger; Vault-backed cron invoker; retrying Edge Function with stale-claim recovery and expired-subscription cleanup; PWA push/click worker; Settings states for unsupported/unconfigured/denied/subscribed devices; per-device enable/disable and preview; foreground duplicate suppression; production VAPID key pair and protected secret storage; Edge Function deployment; migration-ledger apply; Vercel production config and redeploy; service-worker readiness fix that awaits an active worker before Push API subscription; local development guard that disables Web Push when `next-pwa` is disabled; 10-second production readiness timeout; iOS activation-race fix that warms the worker on Settings load and rejects installing/waiting registrations; same-day reminder reschedule migration that versions idempotency by configured time; deployment guide, ADR 007 and OpenWiki updates
- Verification: production and local migration ledgers have 67 aligned versions through `20260830085552`; workflow run `33303304968` applied the reschedule migration from functional master commit `057c64f`; rollback-only production SQL confirms unchanged-time retries insert zero rows while an `08:00` to `08:07` same-day change inserts one new row; the trigger is enabled and its private invoker function has an empty `search_path` with no client execute privilege; both reminder and Web Push cron jobs are active with recent succeeded runs; recent `pg_net` responses are HTTP `200`; delivery audit contains `delivered`; deployment receipt PR `#22` is merged in current master `3ca78ec` and Vercel production deployment `4F8hM9uofeUCfowYqoVLb7wYYzE4`, `https://www.habien.com`, `/sw.js` and the imported push worker all verify successfully; 10 focused notification contract tests, scoped ESLint and root TypeScript pass; linked PL/pgSQL lint reports only the three documented legacy errors
- Constraints: browser permission and subscription are per device; permission must follow a user gesture; iOS/iPadOS Web Push requires a supported installed Home Screen PWA; secrets must never enter Git or client bundles; rotating the VAPID pair requires affected devices to subscribe again
- Risks: a subscription endpoint remains globally unique, so account switching on one browser may require explicit unsubscribe/re-subscribe handling; local Edge HTTP smoke is blocked by the stopped legacy Kong gateway on port `54321`; full repo lint still contains unrelated legacy errors/generated noise; local PL/pgSQL lint retains three unrelated legacy function errors; native packaging and real-device smoke remain separate release gates
- Blockers: final notification appearance requires a real subscribed device outside the open app; operating-system delivery remains device-controlled
- Next smallest step: on production, set an enabled habit reminder at least five minutes in the future, close the app/PWA, and confirm the system notification opens the expected habit route
